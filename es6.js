const numbers = [34, 54, 45, 65, 95];

const person = {
    name: 'Rakib', 
    age: 27,
    profession: 'web developer'
}
// 1. template string
const output = `my name is ${person.name} and my age is ${person.age} my profession is ${person.profession} array third index is ${numbers[2]}`;
console.log(output);

// arrow function
